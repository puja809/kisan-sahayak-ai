server:
  port: ${GATEWAY_SERVICE_PORT:8080}
spring:
  application:
    name: api-gateway
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:prod}
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
